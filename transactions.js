function get() {
    // Get the transactions data
}

function create(transaction) {
    // Create a transactions here
}

function update(id, transaction) {
    // update the transaction data now. 
}

function remove(id) {
    // Delete the transaction
}
